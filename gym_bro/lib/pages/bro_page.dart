import 'package:flutter/material.dart';

class BroPage extends StatelessWidget {
  const BroPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(appBar: AppBar(title: Text("Bro")));
  }
}
