import 'package:flutter/material.dart';

class ScaanPage extends StatelessWidget {
  const ScaanPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(appBar: AppBar(title: Text("Scann")));
  }
}
